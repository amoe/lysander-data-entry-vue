import {
    INTERFACES_FILE_VERSION, AggregatedForm, Neo4jParameters
} from '@/interfaces';

console.log(INTERFACES_FILE_VERSION);


export function toNeo4jParameters(form: AggregatedForm): Neo4jParameters {
    return {};
}
